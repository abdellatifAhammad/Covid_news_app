import React, { Component } from "react";
import { View, Text } from "react-native";

class Card extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
  <View></View>
    );
  }
}

export default Card;
