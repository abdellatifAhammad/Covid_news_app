import React, { Component } from "react";
import { View, Text } from "react-native";

class Tabs extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <View>
        <Text> Tabs </Text>
      </View>
    );
  }
}

export default Tabs;
